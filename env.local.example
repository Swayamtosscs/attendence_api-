# MongoDB Connection
# For local development: Use localhost or install MongoDB locally
# For production (Ubuntu): Use localhost (MongoDB on same server)
MONGODB_URI=mongodb://Toss:Toss%40123@103.14.120.163:27017/attendence?authSource=admin
# Local development example: MONGODB_URI=mongodb://localhost:27017/attendence
# Production example: MONGODB_URI=mongodb://Toss:Toss%40123@localhost:27017/attendence?authSource=admin

AUTH_SECRET=please-change-me

# JWT / auth helpers
AUTH_TOKEN_EXPIRES_IN=7d

# Email (configure if you wire up notifications)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
EMAIL_FROM="Attendance App" <no-reply@your-domain.com>

